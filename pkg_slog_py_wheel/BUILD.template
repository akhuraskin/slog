load("//pkg_slog_py_wheel:pkg_py.bzl", "pkg_py_all_versions")

pkg_py_all_versions(
    # Target name sets the PyPI package name.
    # Install with: pip3 install slog-py --extra-index-url <repo>/simple
    name = "slog_py",
    libs = ["//slog_py"],
    version = "<version>",
)
